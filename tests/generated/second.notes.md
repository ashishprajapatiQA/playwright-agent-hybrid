- The test checks for the presence of the Google logo image (`img` role with name "Google") instead of a traditional heading, as Google's homepage doesn't use semantic headings.
- The logo is a stable selector because it has an accessible name ("Google") and is unlikely to change.
In this case, the test verifies the logo is visible as a proxy for confirming the page loaded correctly.